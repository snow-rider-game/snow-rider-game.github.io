// Intentionally empty: the GameDistribution ad SDK is not used in this
// self-hosted build. The Unity build requests this path, so an empty file
// here avoids a 404 on every load. All gdsdk calls are no-ops without it.
